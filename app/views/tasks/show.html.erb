<h2><%= @task.title %></h2>

<%= link_to 'Edit this task', edit_path(@task) %>

<div id="details">
  <%= @task.details %>
</div>

<div id="completed">
  <%= @task.completed ? '✅ This task is done' : '❌ This task is not completed yet' %>
</div>

<br>
<%= link_to 'Back to list', tasks_path %>
